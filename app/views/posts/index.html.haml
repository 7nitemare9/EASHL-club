
 
- @posts.each do |post| 
  .box
    .content
      %p
        %img{ :src => "assets/blank.jpg", :width => "675" }
        %p2 #{ post.title }

      %p3= raw post.text 
      - if @current_user.user_name == "admin"
        %p
          = link_to 'Edit', edit_post_path(post) 
          = link_to 'Delete', post, confirm: 'Are you sure?', 'data-method' => :delete  

